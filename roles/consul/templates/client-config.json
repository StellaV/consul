{
  "bootstrap": false,
  "server": false,
  "data_dir": "{{ data_dir }}",
  "log_level": "{{ log_level }}",
  "bind_addr": "{{ ansible_default_ipv4.address }}",
  "encrypt": "{{ encrypt }}",
  "start_join": ["{{ cluster_ips }}"],
  "enable_script_checks": true
}
